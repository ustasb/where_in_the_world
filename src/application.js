// Generated by CoffeeScript 1.6.3
(function() {
  var App, LightBox, LocationQuiz, Map, Menu;

  Map = (function() {
    var BACKGROUND_COLOR, SELECTED_REGION_COLOR;

    BACKGROUND_COLOR = '#2980B9';

    SELECTED_REGION_COLOR = '#2ECC71';

    Map.loadMap = (function() {
      var loaded;
      loaded = {};
      return function(mapName, callback) {
        if (loaded[mapName]) {
          return callback();
        } else {
          return $.get("vendor/jvectormap/maps/" + mapName + ".js", function(loadScript) {
            eval(loadScript);
            loaded[mapName] = true;
            return callback();
          });
        }
      };
    })();

    function Map(container_id, mapName) {
      this.el = $('#' + container_id);
      this.mapName = mapName;
    }

    Map.prototype.render = function() {
      var _this = this;
      return Map.loadMap(this.mapName, function() {
        return _this._createMap();
      });
    };

    Map.prototype.selectRegion = function(regionCode) {
      return this.map.setSelectedRegions(regionCode);
    };

    Map.prototype.bindEvents = function(events) {
      var callback, event, _results;
      _results = [];
      for (event in events) {
        callback = events[event];
        _results.push(this.el.bind("" + event + ".jvectormap", callback));
      }
      return _results;
    };

    Map.prototype._createMap = function(opts) {
      this.el.empty();
      this.el.vectorMap({
        map: this.mapName,
        backgroundColor: BACKGROUND_COLOR,
        regionStyle: {
          selected: {
            fill: SELECTED_REGION_COLOR
          }
        }
      });
      return this.map = this.el.vectorMap('get', 'mapObject');
    };

    return Map;

  })();

  LightBox = (function() {
    var backdrop;

    backdrop = $('<div class="light-box-bg"></div>');

    LightBox.showBackdrop = (function() {
      var added;
      added = false;
      return function() {
        if (added) {
          return backdrop.show();
        } else {
          $(document.body).append(backdrop);
          return added = true;
        }
      };
    })();

    LightBox.hideBackdrop = function() {
      return backdrop.hide();
    };

    function LightBox(container_id) {
      this.el = $('#' + container_id);
      this._centerInWindow();
      this.show();
      this._bindEvents();
    }

    LightBox.prototype.hide = function() {
      LightBox.hideBackdrop();
      return this.el.hide();
    };

    LightBox.prototype.show = function() {
      LightBox.showBackdrop();
      return this.el.show();
    };

    LightBox.prototype._bindEvents = function() {
      var _this = this;
      return $(window).resize(function() {
        return _this.centerInWindow();
      });
    };

    LightBox.prototype._centerInWindow = function() {
      var $window;
      $window = $(window);
      return this.el.css({
        left: ($window.width() - this.el.width()) / 2,
        top: ($window.height() - this.el.height()) / 2
      });
    };

    return LightBox;

  })();

  Menu = (function() {
    function Menu(opts) {
      this.onSelectMap = opts.onSelectMap;
      this.onStartQuiz = opts.onStartQuiz;
      this._createMenu();
      this._bindEvents();
    }

    Menu.prototype.show = function() {
      return this.lightbox.show();
    };

    Menu.prototype.hide = function() {
      return this.lightbox.hide();
    };

    Menu.prototype.getSelectedMap = function() {
      return $('#map-type').find(':selected').val();
    };

    Menu.prototype._createMenu = function() {
      return this.lightbox = new LightBox('menu');
    };

    Menu.prototype._bindEvents = function() {
      var _this = this;
      $('#start-quiz').click(this.onStartQuiz);
      return $('#map-type').change(function() {
        return _this.onSelectMap(_this.getSelectedMap());
      });
    };

    return Menu;

  })();

  LocationQuiz = (function() {
    function LocationQuiz(map) {
      this.map = map;
      this._bindEvents();
    }

    LocationQuiz.prototype._bindEvents = function() {
      return this.map.bindEvents({
        regionClick: $.proxy(this._onRegionClick, this)
      });
    };

    LocationQuiz.prototype._onRegionClick = function(e, regionCode) {
      return this.map.selectRegion(regionCode);
    };

    return LocationQuiz;

  })();

  App = (function() {
    var MAP_CONTAINER_ID;

    MAP_CONTAINER_ID = 'map-container';

    function App() {
      var menu,
        _this = this;
      menu = new Menu({
        onSelectMap: this.renderMap,
        onStartQuiz: function() {
          menu.hide();
          return new LocationQuiz(_this.map);
        }
      });
      this.renderMap(menu.getSelectedMap());
    }

    App.prototype.renderMap = function(mapName) {
      this.map = new Map(MAP_CONTAINER_ID, mapName);
      return this.map.render();
    };

    return App;

  })();

  $(document).ready(function() {
    return new App;
  });

}).call(this);
